From: RG35XX Java Platform
Task: RGJ-RC1-011R / 011B / RGJ-RC1-011AS / RGJ-RC1-011AT
Subject: [PATCH] pinned MobilePlatform dirty producers and Libretro input engine

This executable diff owns only the pinned dirty-frame producer points and exact
Libretro joypad/repeat call sites. 0010 remains the lifecycle load-transaction
owner; 0017 remains native-media event drain + EOF owner. No dirty consumer is
added to the blocking LibretroIO parser thread.

RC1-011AT preserves all payloads and converts the last fuzz-dependent regions
to exact zero-context insertion/replacement hunks against the pre-0020 tree.

--- a/src/org/recompile/mobile/MobilePlatform.java
+++ b/src/org/recompile/mobile/MobilePlatform.java
@@ -157,0 +158,3 @@
+
+		/* All replacement LCD/display references are committed at this point. */
+		RG35XXFrameScheduler.markDirty();
@@ -891,6 +894,7 @@
 				gcFrontbuffer.flushGraphics(img, x, y, width, height);
 			}
 			if(postDraw != null) { postDraw.run(); postDraw = null; }
+			RG35XXFrameScheduler.markDirty();
 			painter.run();
 
 			if(focusCommandBar)
--- a/src/org/recompile/freej2me/Libretro.java
+++ b/src/org/recompile/freej2me/Libretro.java
@@ -19,6 +19,7 @@
 
 import org.recompile.mobile.Mobile;
 import org.recompile.mobile.MobilePlatform;
+import org.recompile.mobile.RG35XXInputEngine;
 import org.recompile.mobile.RG35XXLifecycle;
 
 import java.io.File;
@@ -41,4 +42,22 @@
 	private static volatile long lastCoreUpdateTime = System.currentTimeMillis(); // Tracks last core update for pause checks
 
+	/* Slots 0..19 are the RG35XX joypad domain. Mobile.getMobileKey remains the
+	 * single profile/numeric mapping owner at this reusable dispatch boundary. */
+	private static final RG35XXInputEngine.Sink RG35XX_INPUT_SINK = new RG35XXInputEngine.Sink()
+	{
+		public void keyPressed(int slot)
+		{
+			MobilePlatform.keyPressed(Mobile.getMobileKey(slot));
+		}
+		public void keyReleased(int slot)
+		{
+			MobilePlatform.keyReleased(Mobile.getMobileKey(slot));
+		}
+		public void keyRepeated(int slot)
+		{
+			MobilePlatform.keyRepeated(Mobile.getMobileKey(slot));
+		}
+	};
+
 	private byte[] frameBuffer = new byte[800*800*3];
 	private final byte[] frameHeader = new byte[]{(byte)0xFE,
@@ -142,2 +161,3 @@
-								MobilePlatform.pressedKeys[code] = false;
-								MobilePlatform.keyReleased(Mobile.getMobileKey(code));
+								if(code < 0 || code >= 20) { break; }
+								MobilePlatform.pressedKeys[code] = false;
+								RG35XXInputEngine.release(code, RG35XX_INPUT_SINK);
@@ -147,2 +167,3 @@
-								MobilePlatform.pressedKeys[code] = true;
-								MobilePlatform.keyPressed(Mobile.getMobileKey(code));
+								if(code < 0 || code >= 20) { break; }
+								MobilePlatform.pressedKeys[code] = true;
+								RG35XXInputEngine.press(code, System.currentTimeMillis(), RG35XX_INPUT_SINK);
@@ -375,7 +396,3 @@
-
-								// Fire repeats for any currently held keys on
-								// every libretro frame tick
-								for(int i = 0; i < MobilePlatform.pressedKeys.length; i++)
-								{
-									if(MobilePlatform.pressedKeys[i]) { MobilePlatform.keyRepeated(Mobile.getMobileKey(i)); }
-								}
+								/* One bounded repeat update per existing frame tick. Slot 20 remains
+								 * upstream fast-forward state and never enters the input engine. */
+								RG35XXInputEngine.update(lastCoreUpdateTime, RG35XX_INPUT_SINK);
