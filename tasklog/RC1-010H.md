# RGJ-RC1-010H — Native typed media-event callback contract

- Action: ADD / MODIFY / AUDIT
- Status: IMPLEMENTED
- Pre-change reload: `tasklog/TASKLOG.md`, `tasklog/RC1-TASKLOG.md`, `docs/PLATFORM-SOURCE-REGISTRY.md`, current native tree and mixer/MIDI headers/sources were inspected.
- Non-overlap proof: no native module/header owned a typed `(eventType, playerId, mediaTimeUs)` callback. The existing mixer and MIDI callbacks carried only `playerId`; Java/patch 0014 already reserves event types 1=END_OF_MEDIA and 2=LOOPED.
- Added declaration owner: `native/rg35xx_media_events.h` defines the wire-compatible event IDs and the shared `rg35xx_media_event_cb` callback type.
- Modified: `native/rg35xx_mixer.h/.c` and `native/rg35xx_midi_backend.h/.c` now use the shared typed callback.
- PCM semantics: an actual restart emits LOOPED with media time 0; final completion emits END_OF_MEDIA with the terminal media time and does not reset the reported time before notification.
- MIDI semantics: `rg35xx_midi_backend_mix()` consumes `rg35xx_tsf_take_looped()` only as an event signal; the adapter does not own/decrement loop count. Final completion emits END_OF_MEDIA with worker-reported media time.
- Hot-path rule: no heap allocation, blocking I/O or Java callback is added to mixer/MIDI render paths; the eventual consolidated core callback must only serialize/enqueue the fixed case-14 native-to-Java event packet.
- Remaining integration gate: the consolidated `freej2me_libretro.c` call site must be reconciled to the widened callback before BUILD-PASS. The pending `rg35xx_tsf_worker.c` must implement `take_looped()` exactly once per actual restart.
- BUILD-PASS / DEVICE-TEST-PASS are not claimed.

This supplemental immutable task record exists because the connector response for the large `tasklog/RC1-TASKLOG.md` was truncated during this mutation turn; replacing that file from incomplete content would violate immutable-history rules. It must be merged into the main RC1 tasklog only when the complete file can be read without truncation.
